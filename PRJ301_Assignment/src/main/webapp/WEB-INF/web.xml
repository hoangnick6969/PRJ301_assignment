<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Tên hiển thị -->
    <display-name>FashionShopWeb</display-name>

    <!-- Trang mặc định khởi chạy -->
    <welcome-file-list>
        <welcome-file>home</welcome-file>
    </welcome-file-list>

    <!-- ✅ Filter: Bảo vệ user (cần đăng nhập) -->
    <filter>
        <filter-name>UserAuthenticationFilter</filter-name>
        <filter-class>filter.UserAuthenticationFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>UserAuthenticationFilter</filter-name>
        <url-pattern>/profile/*</url-pattern>
        <url-pattern>/wishlist/*</url-pattern>
        <url-pattern>/cart/*</url-pattern>
        <url-pattern>/checkout</url-pattern>
        <url-pattern>/order-history</url-pattern>
        <url-pattern>/order-success</url-pattern>
        <url-pattern>/review-form</url-pattern>
        <url-pattern>/review-submit</url-pattern>
        <url-pattern>/change-password</url-pattern>
    </filter-mapping>

    <!-- ✅ Filter: Bảo vệ quyền admin -->
    <filter>
        <filter-name>AdminAuthenticationFilter</filter-name>
        <filter-class>filter.AdminAuthenticationFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AdminAuthenticationFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>

    <!-- ✅ Trang lỗi tùy chỉnh -->
    <error-page>
        <error-code>404</error-code>
        <location>/views/common/404.jsp</location>
    </error-page>

    <error-page>
        <error-code>500</error-code>
        <location>/views/common/error.jsp</location>
    </error-page>

</web-app>
